# LLM Provider — set ONE of these (or both for fallback)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Default model identifier (must match the provider above)
LLM_MODEL=claude-sonnet-4-6

# App config
APP_PORT=3000
APP_LOG_LEVEL=info
APP_DATA_DIR=./data
