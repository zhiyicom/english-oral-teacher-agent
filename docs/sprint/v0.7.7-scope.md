# Sprint v0.7.7 Scope — V752-001 cosmetic closeout

> **v0.7.x 真正收官**。v0.7.6 是大 wrap-up（3 axis），但 closeout 跑 live demo 时 operator 在 stderr 看到 1 个 cosmetic bug（V752-001：双重 `[System Context]` 前缀）。本 sprint 把它 2 行清掉，之后正式进 v0.8 Web UI。
> **范围**：**1 个 fix / 2 个文件**（`src/cli.ts` 1 行 + 1 个新增 L3 test 锁住）。无设计变更，无架构变更，无新 fixture，无新 .env 字段。
> **不做**：v0.8 Web UI 任何东西 / 任何其他 cosmetic / v0.7.x backlog 其他项（保持原样，等 v0.8 一起或后续单独 sprint）。

---

## 1. 总览

v0.7.6-scope §2.2 政策 "no scope creep" 把 V752-001 推到了 v0.7.6 之外（因为当时 v0.7.6 L3 test 已经用 `\`[cli] ctx-block:\n[System Context]${sysSeg.dynamic}\`` 这个形状写成 passing，改 CLI 字符串就得起 1 个新 test，按当时 scope 政策不顺手做）。本 sprint 是 v0.7.6 的延伸 closeout：补上 v0.7.6 之前就存在的 1 个 cosmetic 字符串重复。

**v0.7.7 做 1 件事**：

`src/cli.ts:454` 把 `\``[cli] ctx-block:\n[System Context]${sysSeg.dynamic}\n` 模板里多余的 `[System Context]` 前缀去掉（`sysSeg.dynamic` 本身第一行就是 `[System Context]`），并在 `tests/agent/cli-integration.test.ts` 加 1 个 L3 test 锁住 "stderr 的 `ctx-block` 行不出现双前缀"。

**用户配置**：无。

**LLM 协议 / Prompt / Fixture 变更**：无。CLI 不会让 LLM 看到这条 stderr；这条 log 只是 operator 调试用。

---

## 2. 范围

### 2.1 In Scope ✅

| # | 项 | 文件 | 备注 |
|---|---|---|---|
| 1 | **去掉 `[System Context]` 双前缀**：`src/cli.ts:454` 改成 `\`[cli] ctx-block:\n${sysSeg.dynamic}\n`` | `src/cli.ts` | 1 行字符串改动 |
| 2 | **加 L3 test 锁住**：新 it 块验证 stderr `ctx-block` 行不包含 `\[System Context\]\[System Context\]`（双前缀）也不包含 stray `[System Context]` 重复段 | `tests/agent/cli-integration.test.ts` | 1 个新 it |

### 2.2 Out of Scope ❌

- v0.8 Web UI 任何东西 — 下一个 sprint
- v0.7.x backlog 其他项（chars/4 estimator 换 tokenizer-based / memory_search session 内缓存 / B4 cache_control 真实 LLM 重测 / etc.）— 保持原样
- 任何其他 cosmetic / 文案 / 注释清理 — "no scope creep" 政策

---

## 3. 用户可配置字段

**无变更**。

---

## 4. 关键交互流程

无功能变化。本 sprint 不改 prompt、不改 LLM 输入、不改 DB schema、不改 tool 协议、不改 state machine。

唯一变化是 stderr 输出格式：

```
修复前:                            修复后:
[cli] ctx-block:                   [cli] ctx-block:
[System Context][System Context]   [System Context]
- Phase: WARM_UP                   - Phase: WARM_UP
- Elapsed: 0.0 min                 - Elapsed: 0.0 min
...                                ...
```

---

## 5. Definition of Done

| DoD | 项 | 验收 |
|---|---|---|
| DoD #1 | `src/cli.ts:454` 字符串模板去掉重复 `[System Context]` | grep `\[System Context\]\\\$\\{` in src/cli.ts 0 命中 |
| DoD #2 | 新增 L3 test：fresh session 第一 turn stderr `ctx-block` 行**不**包含双前缀 `[System Context][System Context]` | vitest 全绿 |
| DoD #3 | 现有 325/325 tests 仍然全绿（无 regression）| `pnpm test` exit 0 |
| DoD #4 | `pnpm typecheck` 0 错误 | exit 0 |
| DoD #5 | `pnpm lint` 0 错误 | exit 0 |
| DoD #6 | 1 个 commit 包含 code change + test + docs | git log |

---

## 6. 风险与缓解

| 风险 | 触发 | 缓解 |
|---|---|---|
| 改 CLI 字符串后某处依赖双前缀 | 旧测试 / 旧 transcript 文档 | 已有 0 处代码或文档依赖该双前缀（grep 验证）|
| 新增 L3 test 污染现有 fixture | 跑多 turn 触发不同 fixture | 新 it 用最简单的 "hi\nexit" 触发 first-turn ctx-block（v0.7.6-issues §V752-001 用的就是这个）|

---

## 7. 估计工时

| 活动 | 工时 |
|---|---|
| scope + design | 0.1 天 |
| 改 CLI 字符串 + 加 L3 test | 0.1 天 |
| 跑 typecheck/lint/test | 0.1 天 |
| test-report + commit | 0.1 天 |
| **总计** | **~0.4 天**（半天最多了）|

---

## 8. 不在 v0.7.7 但应记下的后续

来源：v0.7.6-scope §8 + v0.7.6-issues §Summary + memory `project_sprint_status.md`：

- **v0.8** (next, 5-7 天): Web UI — 主界面 + session 窗口 + 历史列表 + 设置面板 + Playwright E2E
- **v0.8+** (per PRD F-list): F6 voice (v0.9) / F7 topic library ≥20 / F8 session archive
- **v0.8+** (tool expansion): E1 `mark_vocabulary` / E2 `mark_homework`
- **v1.0** (PRD §7 complete): C 组全部错误处理 + 设置 UI + OpenClaw migration + README + GitHub Release + CI
- **持续 backlog**: chars/4 estimator 换 tokenizer-based（如果数据暴露偏差大）/ `memory_search` 结果 session 内缓存

---

## 9. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-06-11 | 0.1 | 初稿：1 个 cosmetic fix / 2 文件 / 半天工时 / 6 条 DoD |
