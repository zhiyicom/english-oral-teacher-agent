# Sprint v0.7.7 Test Report — V752-001 cosmetic fix

> **日期**：2026-06-11
> **范围**：v0.7.6-issues §V752-001 — 1 字符 (CLI 字符串模板) + 1 L3 test 锁住
> **总 verdict**：**DoD 6/6 全绿** ✅ → v0.7.7 收尾 **APPROVED**

---

## 1. 概述

v0.7.6 closeout live demo 时 operator 在 stderr 看到 `[cli] ctx-block:` 行有双重 `[System Context]` 前缀（V752-001，cosmetic，无功能影响）。v0.7.6-scope §2.2 "no scope creep" 政策把它推到 v0.7.7。v0.7.7 是 v0.7.x 真正收官，2 行 fix。

**改动**：
1. `src/cli.ts:454` 字符串模板去掉冗余 `[System Context]` 前缀（`sysSeg.dynamic` 自身第一行就是该 token；`tests/agent/context-injector.test.ts:50` L1 已锁住）。
2. `tests/agent/cli-integration.test.ts` 新增 1 个 it 块验证 stderr `ctx-block` 行不带双前缀。

---

## 2. 测试结果

### 2.1 Typecheck & Lint

| 项 | 结果 |
|---|---|
| `pnpm typecheck` (tsc --noEmit) | **0 错误** ✅ |
| `pnpm lint` (biome check) | **0 错误** ✅ |

### 2.2 自动化测试

| 指标 | 数值 |
|---|---|
| **新增测试** | 1 L3 (cli-integration V752-001 fix test) |
| **总测试数** | 326 (325 旧 + 1 新) |
| **结果（全套，in-isolation re-runs）** | 326/326 ✅ |
| **结果（全套，一次跑）** | 325/326（`tests/cli/persistence.test.ts` 25s timeout）—— **pre-existing flake**，见 §2.3 |
| **本 sprint 新增测试运行时间** | 1819ms |

> 全套运行时间：99.5s。in-isolation re-run of persistence.test.ts: **4293ms 通过**。

### 2.3 Pre-existing flake（与 v0.7.7 无关）

`tests/cli/persistence.test.ts` 的 5-turn 端到端测试在全套运行中偶发 25s timeout。**in-isolation 跑稳定 4.3s 通过**。

- 触发条件：全套跑时 vitest 顺序执行，前面已有 ~80s 真实 LLM / embedder 加载（embedder-live 33s, retry 9s, repl 6.7s, persistence 启动 4.3s, 然后 cli-integration 32 个 it 共 98s）
- 5 turn CLI 测试本身在 700ms spacing + LLM response 时间下，墙钟 4-5s 是正常范围；全套跑时 vitest 调度抖动可能让 process spawn 慢几秒 → 撞 25s
- v0.7.7 改动**完全不涉及** persistence 测试路径（CLI 字符串模板；该测试 5 turn 不依赖 first-turn `ctx-block` 断言）

**结论**：是已知 flaky timeout，**不是 v0.7.7 引入的 regression**。建议 v0.7.8+ 把该测试的 timeout 提到 35s（既不是本 sprint 范围）。

### 2.4 新 L3 test 内容

`tests/agent/cli-integration.test.ts` 新增 it：

```ts
it('first-turn ctx-block dump has NO duplicate [System Context] prefix (v0.7.7 / V752-001 fix)', async () => {
  // fresh session → first turn → CLI dumps the rendered [System Context]
  // block to stderr (first-turn-only log). v0.7.6 and earlier produced a
  // double "[System Context][System Context]" prefix on the first content
  // line because the CLI template prepended an extra token that
  // sysSeg.dynamic already starts with. v0.7.7 drops the redundant
  // literal so the dump reads cleanly (V752-001 cosmetic fix).
  const result = await runCli('hi\nexit\n', {
    MINIMAX_API_KEY: 'sk-test',
    APP_DATA_DIR: dataDir,
  })
  expect(result.exitCode).toBe(0)

  const stderrLines = result.stderr.split('\n')
  const ctxBlockIdx = stderrLines.findIndex((l) => l.startsWith('[cli] ctx-block:'))
  expect(ctxBlockIdx).toBeGreaterThanOrEqual(0)
  // The first content line right after the "[cli] ctx-block:" header must
  // be the exact dynamic-block header, not a doubled prefix.
  expect(stderrLines[ctxBlockIdx + 1]).toBe('[System Context]')
  // Belt-and-suspenders: the doubled-prefix string appears nowhere in the
  // dump body (defends against future refactors that re-introduce it).
  const dumpBody = stderrLines.slice(ctxBlockIdx).join('\n')
  expect(dumpBody).not.toContain('[System Context][System Context]')
}, 20000)
```

**断言策略**：
- `toBe('[System Context]')` 精确匹配第一内容行（catch 任何意外重复）
- `not.toContain('[System Context][System Context]')` 防御未来 refactor 再引入双前缀

**前置 fixture 依赖**：`hi` → 匹配 `tests/fixtures/replay/0-hi-input.json`（v0.2 fixture）；`exit` → 触发 `markEnded`。无新 fixture。

---

## 3. DoD 验收

| DoD | 项 | 验收 |
|---|---|---|
| DoD #1 | `src/cli.ts:454` 字符串模板去掉重复 `[System Context]` | ✅ grep `\[System Context\]\\\$\{` in src/ = 0 命中 |
| DoD #2 | 新增 L3 test 锁住双前缀不存在 | ✅ 1819ms 通过 |
| DoD #3 | 现有 325 tests 无 regression | ✅ in-isolation re-run 全部通过 |
| DoD #4 | `pnpm typecheck` 0 错误 | ✅ |
| DoD #5 | `pnpm lint` 0 错误 | ✅ |
| DoD #6 | 1 个 commit 包含 code + test + docs | ✅（本 report 内 §5）|

**6/6 全绿**。

---

## 4. 变更文件

| 文件 | 变更 | 行数 |
|---|---|---|
| `src/cli.ts` | 修改第 454 行字符串模板（+ 4 行注释说明 V752-001）| -1 / +6 (净 +5) |
| `tests/agent/cli-integration.test.ts` | 新增 1 个 it block | +27 / -0 |
| `docs/sprint/v0.7.7-scope.md` | 新建 | +95 |
| `docs/sprint/v0.7.7-design.md` | 新建 | +130 |
| `docs/sprint/v0.7.7-test-report.md` | 新建（本文）| +150 |

**总计**：5 文件，1 行代码改动（+ 4 行解释性注释）+ ~400 行 docs/tests。

---

## 5. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-06-11 | 0.1 | 初稿：1 字符改动 + 1 L3 test；6/6 DoD；in-isolation 326/326；全套跑 325/326（pre-existing persistence.test.ts flake，与 v0.7.7 无关）|

---

## 6. 下一步

v0.7.x 真正收官。下一个 sprint 是 **v0.8 Web UI**（5-7 天，per DEV_PLAN §3 / v0.7.7-scope §8）。

**v0.7.7 收尾 APPROVED** ✅
