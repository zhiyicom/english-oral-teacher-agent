# Sprint v0.8.2 设计 — React 19 + Vite 6 骨架 + 主页面

> **Sprint**: v0.8.2（v0.8 Web UI 的 5 个子 sprint 之第 2 个）
> **父范围/设计文档**: [`docs/sprint/v0.8-scope.md`](./v0.8-scope.md) + [`docs/sprint/v0.8-design.md`](./v0.8-design.md)
> **配套范围文档**: [`docs/sprint/v0.8.2-scope.md`](./v0.8.2-scope.md)

---

## 1. 技术栈（由 v0.8-design §1.2 + §4.1 锁定）

| 依赖 | 版本 | 原因 |
|---|---|---|
| `react` | ^19.0.0 | 最大生态，PRD F9.1 未来 GitHub 友好 |
| `react-dom` | ^19.0.0 | DOM 渲染器 |
| `react-router-dom` | ^7.0.0 | 客户端路由（4 个路径）|
| `vite` | ^6.0.0 | 最佳开发 HMR |
| `@vitejs/plugin-react` | ^4.3 | React 19 Vite 插件 |
| `tailwindcss` | ^4.0 | 零配置 CSS 框架（v4 原生支持 Vite 6）|
| `@tailwindcss/vite` | ^4.0 | Tailwind v4 Vite 插件 |
| `@types/react` | ^19.0.0 | React 19 类型 |
| `@types/react-dom` | ^19.0.0 | React DOM 19 类型 |
| `typescript` | ^5.7 | web 端 TS（服务端有独立版本）|
| `@playwright/test` | ^1.49 | E2E（v0.8.2 1 个 spec）|
| `concurrently`（根 devDep）| ^9.0 | `pnpm dev-web` 编排（约 6KB）|

**新增运行时体积**：0（全部为 dev deps）。打包体积约 50-80KB gzipped（估算）。

---

## 2. 文件结构

```
D:\English-oral-teacher-Agent\
├── web/                                    # [新增] Vite + React 19 子项目
│   ├── package.json                        # [新增] 独立依赖
│   ├── vite.config.ts                      # [新增] React + Tailwind + 代理
│   ├── tsconfig.json                       # [新增] JSX + DOM + strict
│   ├── tsconfig.node.json                  # [新增] 用于 vite.config.ts
│   ├── index.html                          # [新增] SPA 外壳
│   └── src/
│       ├── main.tsx                        # [新增] React 19 createRoot
│       ├── App.tsx                         # [新增] BrowserRouter + 4 个路由
│       ├── index.css                       # [新增] @import "tailwindcss"
│       ├── vite-env.d.ts                   # [新增] /// <reference types="vite/client" />
│       ├── components/
│       │   ├── MainPage.tsx                # [新增] 真实实现
│       │   ├── SessionPage.tsx             # [新增] 占位
│       │   ├── HistoryPage.tsx             # [新增] 占位
│       │   ├── SettingsPage.tsx            # [新增] 占位
│       │   └── shared/
│       │       └── Header.tsx              # [新增] 通用 header
│       ├── lib/
│       │   ├── api.ts                      # [新增] fetch 封装
│       │   └── types.ts                    # [新增] SessionApi 镜像
│       └── i18n/
│           └── strings.ts                  # [新增] 中文字符串 + 英文桩
├── tests/
│   └── e2e/
│       └── main.spec.ts                    # [新增] E2E #1
├── playwright.config.ts                    # [新增] 根目录，webServer 运行 pnpm dev-web
├── package.json                            # [修改] 添加 concurrently + 4 个脚本 + 扩展 typecheck
├── biome.json                              # [修改] 扩展 files.ignore
├── .gitignore                              # [修改] 添加 web 产物 + playwright 产物
├── docs/
│   ├── ARCHITECTURE.md                     # [修改] §11 变更记录
│   └── sprint/
│       ├── v0.8.2-scope.md                 # [新增]
│       ├── v0.8.2-design.md                # [新增，本文件]
│       └── v0.8.2-test-report.md           # [新增，收尾时撰写]
└── （现有 src/、tests/server/ 等 — 不变）
```

**为什么 `web/` 有独立 `package.json`**（不是 monorepo）：v0.8-design §2.2 — 单次根 `pnpm install` 安装服务端依赖，然后 `pnpm --dir web install` 安装 web 依赖。保持服务端 lockfile 干净。

---

## 3. 关键文件契约

### 3.1 `web/vite.config.ts` — 代理是关键

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    port: 5173,
    strictPort: true,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
    },
  },
})
```

`strictPort: true` — 5173 被占用时快速失败（优于静默选择其他端口导致 Playwright 困惑）。

### 3.2 `web/tsconfig.json` — 独立于服务端

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "allowImportingTsExtensions": false
  },
  "include": ["src/**/*"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### 3.3 `web/src/lib/api.ts` — 类型化 fetch 封装

```ts
import type { SessionApi } from './types'

// BASE 在开发（Vite 代理）和生产（打包后同源）中均为空。
// 这意味着永不发生 CORS；所有浏览器 fetch 都在自身 origin 上。
const BASE = ''

export async function listSessions(): Promise<SessionApi[]> {
  const res = await fetch(`${BASE}/api/sessions`)
  if (!res.ok) throw new Error(`listSessions: HTTP ${res.status}`)
  const body = (await res.json()) as { sessions: SessionApi[] }
  return body.sessions
}

export async function createSession(): Promise<{ id: string }> {
  const res = await fetch(`${BASE}/api/sessions`, { method: 'POST' })
  if (!res.ok) throw new Error(`createSession: HTTP ${res.status}`)
  return (await res.json()) as { id: string }
}

export async function getSession(id: string): Promise<SessionApi> {
  const res = await fetch(`${BASE}/api/sessions/${encodeURIComponent(id)}`)
  if (!res.ok) throw new Error(`getSession: HTTP ${res.status}`)
  return (await res.json()) as SessionApi
}
```

### 3.4 `web/src/lib/types.ts` — 服务端 `SessionApi` 镜像

```ts
export interface SessionApi {
  id: string
  startedAt: string
  endedAt: string | null
  durationMin: number | null
  phaseHistory: string[]
  summary: string | null
  keywords: string[]
  topicMatch: string | null
}
```

此接口与服务端 `src/server.ts` 中的 `SessionApi` 重复。v0.8.2 手动保持两者同步。v0.8.5+ 如有 monorepo 可提取到 `packages/shared/types.ts`。

### 3.5 `web/src/App.tsx` — Router + 4 个路由

```tsx
import { BrowserRouter, Route, Routes } from 'react-router-dom'
import Header from './components/shared/Header'
import MainPage from './components/MainPage'
import SessionPage from './components/SessionPage'
import HistoryPage from './components/HistoryPage'
import SettingsPage from './components/SettingsPage'

export default function App() {
  return (
    <BrowserRouter>
      <div className="min-h-screen bg-slate-50">
        <Header />
        <main className="mx-auto max-w-4xl px-4 py-8">
          <Routes>
            <Route path="/" element={<MainPage />} />
            <Route path="/session/:id" element={<SessionPage />} />
            <Route path="/history/:id" element={<HistoryPage />} />
            <Route path="/settings" element={<SettingsPage />} />
          </Routes>
        </main>
      </div>
    </BrowserRouter>
  )
}
```

### 3.6 `web/src/components/MainPage.tsx` — 首个真实页面

```tsx
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { createSession, listSessions } from '../lib/api'
import type { SessionApi } from '../lib/types'
import { STRINGS } from '../i18n/strings'

export default function MainPage() {
  const navigate = useNavigate()
  const [sessions, setSessions] = useState<SessionApi[] | null>(null)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    listSessions()
      .then(setSessions)
      .catch((e: Error) => setError(e.message))
  }, [])

  async function handleStart() {
    try {
      const { id } = await createSession()
      navigate(`/session/${id}`)
    } catch (e) {
      setError((e as Error).message)
    }
  }

  if (error) {
    return (
      <div className="rounded border border-red-300 bg-red-50 p-4" data-testid="error-banner">
        <p className="text-red-700">{STRINGS.errorPrefix}: {error}</p>
        <button
          onClick={() => window.location.reload()}
          className="mt-2 rounded bg-red-600 px-3 py-1 text-white"
        >
          {STRINGS.retry}
        </button>
      </div>
    )
  }

  if (sessions === null) {
    return <p data-testid="loading">{STRINGS.loading}</p>
  }

  return (
    <div>
      <button
        onClick={handleStart}
        data-testid="start-button"
        className="rounded bg-blue-600 px-4 py-2 text-white hover:bg-blue-700"
      >
        {STRINGS.startPractice}
      </button>

      {sessions.length === 0 ? (
        <p className="mt-8 text-slate-500" data-testid="empty-state">
          {STRINGS.emptyState}
        </p>
      ) : (
        <ul className="mt-8 space-y-3" data-testid="session-list">
          {sessions.map((s, i) => (
            <li
              key={s.id}
              data-testid="session-row"
              className="cursor-pointer rounded border bg-white p-4 shadow-sm hover:shadow"
              onClick={() => navigate(`/history/${s.id}`)}
            >
              <div className="flex items-baseline justify-between">
                <span className="font-mono text-sm text-slate-500">
                  #{sessions.length - i} · {new Date(s.startedAt).toLocaleString()}
                </span>
                {s.durationMin !== null && (
                  <span className="text-sm text-slate-500">
                    {s.durationMin} {STRINGS.minutesShort}
                  </span>
                )}
              </div>
              {s.summary && (
                <p className="mt-2 text-slate-700">
                  {s.summary.length > 80 ? `${s.summary.slice(0, 80)}…` : s.summary}
                </p>
              )}
              {s.keywords.length > 0 && (
                <div className="mt-2 flex flex-wrap gap-1">
                  {s.keywords.map((k) => (
                    <span
                      key={k}
                      className="rounded-full bg-slate-100 px-2 py-0.5 text-xs text-slate-600"
                    >
                      {k}
                    </span>
                  ))}
                </div>
              )}
            </li>
          ))}
        </ul>
      )}
    </div>
  )
}
```

### 3.7 占位页面 — 统一形态

```tsx
// SessionPage.tsx（代表性示例）
import { useParams } from 'react-router-dom'
import { STRINGS } from '../i18n/strings'

export default function SessionPage() {
  const { id } = useParams()
  return (
    <div data-testid="placeholder">
      <h2 className="text-xl">{STRINGS.sessionTitle}</h2>
      <p className="mt-2 text-slate-500">{STRINGS.placeholderSession(id ?? '')}</p>
    </div>
  )
}
```

HistoryPage 和 SettingsPage 采用相同形态，仅 i18n 字符串不同。

### 3.8 `web/src/i18n/strings.ts` — 中文 + 英文桩

```ts
export const STRINGS = {
  appTitle: 'English Oral Teacher',
  startPractice: '开始新练习',
  loading: '加载中…',
  emptyState: '暂无练习记录 — 开始第一次练习吧',
  errorPrefix: '加载失败',
  retry: '重试',
  minutesShort: '分钟',
  sessionTitle: '练习中',
  historyTitle: '历史记录',
  settingsTitle: '设置',
  placeholderSession: (id: string) =>
    `Session #${id} — 对话窗口将在 v0.8.3 实现`,
  placeholderHistory: (id: string) =>
    `Session #${id} — 历史详情将在 v0.8.4 实现`,
  placeholderSettings: '设置面板将在 v0.8.4 实现',
} as const
```

完整 i18n 切换（中/英）延至 v0.8.5。

### 3.9 `playwright.config.ts`（根目录）

```ts
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',
  timeout: 30_000,
  expect: { timeout: 5_000 },
  fullyParallel: false,
  workers: 1,
  retries: 0,
  reporter: process.env.CI ? 'github' : 'list',
  use: {
    baseURL: 'http://localhost:5173',
    headless: true,
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'pnpm dev-web',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
    stdout: 'pipe',
    stderr: 'pipe',
  },
})
```

`webServer.command = 'pnpm dev-web'` — Playwright 通过 `concurrently` 启动 server (3000) + Vite (5173)，等待 5173 就绪，运行测试，退出时终止进程组。

### 3.10 `tests/e2e/main.spec.ts`

```ts
import { expect, test } from '@playwright/test'

test('main page shows header + start button + empty list', async ({ page }) => {
  await page.goto('/')
  await expect(page.locator('h1')).toContainText('English Oral Teacher')
  await expect(page.getByTestId('start-button')).toBeVisible()
  await expect(page.getByTestId('start-button')).toHaveText('开始新练习')
  await expect(page.getByTestId('empty-state')).toBeVisible()
  await expect(page.getByTestId('session-row')).toHaveCount(0)
})
```

### 3.11 根 `package.json` 脚本补充

```jsonc
{
  "scripts": {
    "dev": "tsx watch src/cli.ts",
    "serve": "tsx watch src/server.ts",
    "dev-web": "concurrently -k -n srv,web -c blue,magenta \"pnpm serve\" \"pnpm --dir web dev\"",
    "build": "pnpm --dir web build && tsc",
    "preview": "pnpm --dir web preview",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p web/tsconfig.json",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live": "RUN_LIVE_LLM=1 vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.0",
    "concurrently": "^9.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
```

`typecheck` 现在运行 **3** 次 `tsc --noEmit`：服务端、测试文件、web。顺序选择使服务端错误（最常见）优先报告。

---

## 4. CSS 策略

**Tailwind v4 零配置** — `web/src/index.css` 仅一行：

```css
@import "tailwindcss";
```

`@tailwindcss/vite` 插件按需生成工具类。无需 `tailwind.config.js`，无需 `postcss.config.js`。v0.8.2 中若遇到工具类缺失，回退到内联 `style={{}}`（例如 `style={{ minHeight: '80vh' }}`）。

深色模式切换延至 v0.8.5。

---

## 5. 组件分层

| 层级 | 工具 | 原因 |
|---|---|---|
| 路由 | `react-router-dom` 7 | v0.8-design §4.1 锁定 |
| 状态 | React 19 hooks（`useState`、`useEffect`）| v0.8-design §4.3 — 4 个端点不需要 React Query |
| 服务端状态 | 直接在 `useEffect` 中 `fetch` | 同上 |
| 表单 | 原生 `<button onClick>` + `<form onSubmit>` | 无需表单库（Settings 延至 v0.8.4）|
| 样式 | Tailwind v4 工具类 | v0.8-design §4.4 锁定 |
| i18n | 单一 `STRINGS` 常量 | 完整 i18n 延至 v0.8.5 |

**组件组合规则**：每个页面自包含；v0.8.2 中仅 `Header` 被共享。

---

## 6. 开发 / 构建 / 运行工作流

### 6.1 首次设置（一次性，记录在测试报告中）

```bash
pnpm install                  # 根依赖（添加 concurrently）
pnpm --dir web install        # web 依赖
npx playwright install chromium  # 约 150MB 下载，后续缓存
```

### 6.2 开发

```bash
pnpm dev-web
# → 同时启动 server (3000) + Vite (5173)
# 浏览器: http://localhost:5173 → 完整 UI
# curl http://localhost:3000/api/sessions → 原始 JSON
```

### 6.3 生产构建

```bash
pnpm build
# → 1. pnpm --dir web build  (vite → web/dist/)
# → 2. tsc                   (server → dist/)
```

### 6.4 E2E 测试

```bash
pnpm test:e2e
# → Playwright 启动 dev-web，等待 5173，运行测试，终止进程
```

### 6.5 预览生产构建

```bash
pnpm --dir web preview
# → 在 http://localhost:4173 上 serve web/dist/
# （src/server.ts 中的生产 SPA fallback 属于 v0.8.5；
#   目前 preview 是测试打包产物的方式。）
```

---

## 7. 测试策略

| 层级 | 工具 | v0.8.2 数量 | 备注 |
|---|---|---|---|
| L1 服务端 | vitest | 0 新增（保持现有 326 基线）| turn.ts / server.ts 不变 |
| L3 服务端 | vitest | 0 新增 | v0.8.1 现有 18 个 |
| **E2E web** | **Playwright** | **1 新增**（`tests/e2e/main.spec.ts`）| webServer 启动 dev-web |
| 组件 L1 | vitest + @testing-library/react | 0（延至 v0.8.5）| — |
| 视觉 / a11y | Playwright 截图 | 0（延至 v0.8.5）| — |

**目标测试数**：344（v0.8.1）+ 1（E2E）= 345。

---

## 8. 风险登记（v0.8.2 专项）

| 风险 | 缓解措施 |
|---|---|
| React 19 + Vite 6 在 Windows 上 peer-dep 不匹配 | 精确锁定版本；先验证 `pnpm --dir web install` 成功 |
| Tailwind v4 零配置意外 | v4 `@tailwindcss/vite` 插件处理配置；有缺口时回退到内联 `style` |
| `concurrently` 在 Windows cmd.exe 上引号问题 | 使用 `concurrently -k -n srv,web` 加简单命令字符串；备选 npm-run-all2 |
| Playwright chromium 首次下载慢 | 一次性 `npx playwright install chromium`；记录在测试报告中 |
| dev-web 启动时 E2E 不稳定 | Playwright `webServer.timeout: 60_000` + `reuseExistingServer: !CI` |
| 开发时 CORS | Vite 代理 `/api` → 3000；浏览器仅见 5173 |
| TypeScript strict + JSX + DOM lib 与服务端 tsconfig 冲突 | 独立 `web/tsconfig.json`；根 `tsconfig.json` 排除 `web/` |
| 工具链碎片化（web 有独立 deps + lockfile）| 按 v0.8-design §2.2 可接受；用户仅运行一次 `pnpm --dir web install` |
| Playwright `webServer` 在 Windows 上子进程清理不完全 | `concurrently -k` 传递 SIGTERM 给两个子进程；v0.8.2 DoD 已验证 |

---

## 9. 已记录决策（对照 v0.8-design §1.2 / §4 基线）

| 决策 | v0.8-design 基线 | v0.8.2 选择 | 原因 |
|---|---|---|---|
| 前端文件夹 | `web/`（依照 v0.8-design §2.1）| 确认 `web/` | 独立 `package.json` |
| React 版本 | ^19.0.0 | ^19.0.0 | 最大生态 |
| Vite 版本 | ^6.0.0 | ^6.0.0 | 最佳 HMR |
| Tailwind | v4 零配置 | v4 零配置 | 无需 `tailwind.config.js` |
| 路由 | React Router 7 | React Router 7 | 客户端路由 |
| 状态管理 | `useState` + 自定义 `useApi` | v0.8.2 用 `useState` + `useEffect`；`useApi` 延后 | 仅 4 个端点；尚不需要 |
| i18n | 中文 + 英文（v0.8.5）| 仅中文 + 英文桩 | v0.8.5 打磨 |
| 开发编排器 | `concurrently -k -n srv,web "pnpm serve" "pnpm --dir web dev"` | 同上 | 6KB 依赖，成熟模式 |
| 构建链 | `pnpm --dir web build && tsc` | 同上 | 两步 |
| Playwright 配置 | 根 `playwright.config.ts` | 同上 | webServer 运行 `pnpm dev-web` |
| E2E #1 范围 | 仅主页面 | 同上 | Session/history/settings E2E 在 v0.8.3/4 |
| WebSocket | PRD 未提及 | 不使用 | SSE 用于流式传输 |
| 组件 L1 测试 | v0.8.5 打磨 | 延后 | — |

---

## 10. 完成定义回顾

（完整 DoD 见 v0.8.2-scope.md §4。此处为参考回顾。）

- `pnpm typecheck` 0 错误（server + test + web）
- `pnpm lint` 0 错误
- `pnpm test` 344/344（仅服务端 vitest）
- `pnpm build` 成功 → 产出 `web/dist/`
- `pnpm test:e2e` 1/1（main.spec.ts 通过）
- `pnpm dev-web` 运行两个进程；浏览器显示 h1 + 开始按钮
- 0 CLI 回归（32/32 L3 cli-integration 仍通过）
- 3 个文档文件已提交（scope + design + test-report）
- ARCHITECTURE.md §11 变更记录已更新
- 0 fixture 删除，0 agent 核心协议变更
