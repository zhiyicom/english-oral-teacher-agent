# Sprint v0.8.2 范围 — React 19 + Vite 6 骨架 + 主页面

> **Sprint**: v0.8.2（v0.8 Web UI 的 5 个子 sprint 之第 2 个）
> **父范围文档**: [`docs/sprint/v0.8-scope.md`](./v0.8-scope.md) §2.1 v0.8.2 块
> **父设计文档**: [`docs/sprint/v0.8-design.md`](./v0.8-design.md) §1.2, §4, §10
> **上一 sprint**: v0.8.1（commit `b969cd0` + `766ae76`）— 服务端骨架已完成
> **分支**: `main`（无 feature branch — v0.8.x 遵循 v0.7.x 模式直接交付到 main）
> **日期**: 2026-06-12

---

## 背景

v0.8.1 交付了**服务端骨架**：`src/agent/turn.ts`（共享 REPL 循环）、`src/server.ts`（Hono HTTP + SSE）、3 个 REST 端点 + 1 个 SSE 桩、18 个新测试（总计 344），以及运行在 3000 端口的 `pnpm serve`。

v0.8.2 是**前端骨架**——学生在浏览器中实际看到的那一半。打开 `http://localhost:5173` → 看到 "English Oral Teacher" + [开始新练习] 按钮 + （空的）会话列表。

三件事必须端到端跑通：

1. **开发编排**：`pnpm dev-web` 同时运行 server (3000) + Vite (5173)；Vite 将 `/api/*` 代理 → 3000，浏览器永不遇到 CORS。
2. **4 个路由带占位页面** + `MainPage` 实际接入 `GET /api/sessions`。
3. **Playwright E2E #1**（主页面）通过。

---

## 1. 范围内

| # | 事项 | 文件 | 备注 |
|---|---|---|---|
| 1 | `web/` 下搭建 Vite 6 + React 19 + TS 脚手架 | `web/package.json`、`web/vite.config.ts`、`web/tsconfig.json`、`web/index.html` | 独立 `web/package.json`，见 v0.8-design §2.2 |
| 2 | Tailwind v4（通过 `@tailwindcss/vite` 零配置）| `web/src/index.css`、`web/vite.config.ts` | 一行 `@import "tailwindcss";`——无需 `tailwind.config.js` |
| 3 | React Router 7，4 个路由（`/`、`/session/:id`、`/history/:id`、`/settings`）| `web/src/main.tsx`、`web/src/App.tsx` | 4 个路由中 3 个可为占位页 |
| 4 | `MainPage` 调用 `GET /api/sessions`，渲染空状态 + 列表行（编号、日期、时长、摘要、关键词）| `web/src/components/MainPage.tsx`、`web/src/lib/api.ts`、`web/src/lib/types.ts` | 点击行 → `/history/:id`；[开始新练习] → `POST /api/sessions` → `/session/:id` |
| 5 | 3 个占位页面（`SessionPage`、`HistoryPage`、`SettingsPage`）| `web/src/components/{SessionPage,HistoryPage,SettingsPage}.tsx` | "v0.8.{3|4} 实现 — 当前为空" |
| 6 | 共享 `Header` 组件 | `web/src/components/shared/Header.tsx` | "English Oral Teacher" 标题 |
| 7 | 根目录 `dev-web` 脚本通过 concurrently 同时运行 server + Vite | 根 `package.json` | 使用 `concurrently`（新增 devDep）|
| 8 | 根目录 `build` 脚本串联 `vite build` + `tsc` | 根 `package.json` | 用于生产部署 |
| 9 | Playwright E2E #1（主页面渲染 h1 + 开始按钮 + 空列表）| `playwright.config.ts`、`tests/e2e/main.spec.ts` | 一次性 `npx playwright install chromium` |
| 10 | `web/` 使用 TypeScript strict（`web/` 中的 `tsconfig.json`）| `web/tsconfig.json` | JSX、DOM lib、bundler 模块解析 |
| 11 | `.gitignore` 新增：`web/dist/`、`web/node_modules/`、`web/.vite/`、`test-results/`、`playwright-report/` | `.gitignore` | |
| 12 | `biome.json` 忽略 web 构建产物 | `biome.json` | `web/dist`、`web/node_modules`、`web/.vite` |
| 13 | Sprint 文档：范围（本文件）+ 设计 + 测试报告 | `docs/sprint/v0.8.2-*.md` | 3 个文档文件 |
| 14 | `docs/ARCHITECTURE.md` §11 变更记录添加 v0.8.2 条目 | `docs/ARCHITECTURE.md` | 一行条目 |

---

## 2. 范围外（保护清单）

| 事项 | 原因 | 移交至 |
|---|---|---|
| SSE turn loop 接线（SessionPage 消费 `/api/sessions/:id/stream`）| 完整 turn loop 需要稳定 turn.ts 事件；v0.8.3 子 sprint | v0.8.3 |
| `HistoryPage` 详情接线（GET /api/sessions/:id 含 `messages[]`）| 服务端端点扩展（`messages` 字段）属于 v0.8.4 | v0.8.4 |
| `SettingsPage` 接线（PUT /api/settings + USER.md 原子写 via proper-lockfile）| 设置子 sprint | v0.8.4 |
| 组件 L1 测试（`@testing-library/react`）| 打磨子 sprint | v0.8.5 |
| 完整 i18n 切换（中/英切换 UI）| 打磨子 sprint | v0.8.5 |
| 深色模式、完整 a11y 审查（WCAG AA）、1280×720 媒体查询打磨 | 打磨子 sprint | v0.8.5 |
| `src/server.ts` 生产 SPA fallback（serve `web/dist/index.html`）| 在 `web/dist` 存在后触发；不阻塞 v0.8.2 | v0.8.5 或 v1.0 |
| F6 语音 I/O（STT/TTS/队列）| PRD F6 明确标 v0.9 | v0.9 |
| F7 话题库扩展（≥20 个话题）| PRD F7 积压 | v1.0 |
| F8 会话归档 `.md` 写入 | PRD F8 明确标 v1.0 | v1.0 |
| Tauri 桌面壳 | PRD F9.1 — 仅纯 Web | 不做 |
| WebSocket | PRD 未提及 | 不做 |
| 新服务端端点 | v0.8.2 仅消费 v0.8.1 的 3 个 REST + 1 个 SSE 桩 | — |
| Tailwind 主题配置 / `tailwind.config.js` | v4 为零配置；无需配置文件 | — |
| pnpm workspace（monorepo）| v0.8-design §2.2 明确拒绝 | 不做 |
| React Query / Zustand / Redux | 按 v0.8-design §4.3 使用 `useState` + `useEffect` | 不做 |

---

## 3. 用户可配置字段

v0.8.2 无新增 `.env` 字段。

`PORT`（默认 3000）已在 v0.8.1 添加，继续适用。

---

## 4. 完成定义

### 4.1 Sprint DoD（依据 v0.8-scope §5.3）

| # | 标准 | 验证方式 |
|---|---|---|
| 2.1 | `pnpm dev-web` 同时运行 server + Vite | 手动：两个后台进程，终端中均可见 |
| 2.2 | 浏览器访问 `http://localhost:5173` 看到 h1 "English Oral Teacher" + [开始新练习] 按钮 + （空的）会话列表 | 手动 + Playwright E2E |
| 2.3 | Playwright E2E #1（main.spec.ts）通过 | `pnpm test:e2e` exit 0 |

### 4.2 交叉 DoD（v0.8-scope §5.1 G-DoD）

| # | 标准 | 验证方式 |
|---|---|---|
| G-1 | `pnpm typecheck` 0 错误（server + web + test）| 3 次 `tsc --noEmit` 调用 |
| G-2 | `pnpm lint` 0 错误（biome）| biome 可对新 web 文件自动修复 |
| G-3 | `pnpm test` 344/344（服务端 vitest）| 无回归 |
| G-4 | `pnpm build` 成功（`vite build` + `tsc`）| 产出 `web/dist/` |
| G-5 | `pnpm test:e2e` 通过 | Playwright exit 0 |
| G-8 | `pnpm dev`（CLI）仍可运行 | 回归 — v0.7.7 cli-integration 32/32 仍通过 |
| G-9 | 3 个文档文件落地（`scope` + `design` + `test-report`）| git diff |
| G-11 | 0 fixture 删除，0 agent 核心协议变更 | git diff |

### 4.3 真实演示流程（DoD 门）

```
1. pnpm install                                 # 添加 concurrently
2. pnpm --dir web install                       # web 依赖
3. npx playwright install chromium              # 一次性，约 150MB 下载
4. pnpm dev-web                                 # server (3000) + vite (5173)
5. 浏览器: http://localhost:5173/
   - h1 "English Oral Teacher"
   - [开始新练习] 按钮（可见、可点击）
   - 空状态: "暂无练习记录"
6. 点击 [开始新练习]
   - 浏览器导航至 /session/<新id>
   - SessionPage 占位页渲染
7. curl http://localhost:3000/api/sessions
   - 返回 1 行，含新会话
```

---

## 5. 工时估算

依据 v0.8-scope §7：1 天。子任务：

| # | 子任务 | 预估 |
|---|---|---|
| 1 | Sprint 文档（范围 + 设计）| 0.5 小时 |
| 2 | `web/` 脚手架（package.json + vite.config + tsconfig + index.html）| 1 小时 |
| 3 | `web/src/` 外壳（main.tsx + App.tsx + index.css + vite-env.d.ts）| 0.5 小时 |
| 4 | 4 个路由占位页 + Header | 0.5 小时 |
| 5 | `lib/api.ts` + `lib/types.ts` | 0.5 小时 |
| 6 | `MainPage`（真实实现：列表 + 开始按钮 + 导航）| 1.5 小时 |
| 7 | 根目录脚本 + concurrently + tsconfig 扩展 | 0.5 小时 |
| 8 | Playwright 配置 + main.spec.ts | 1 小时 |
| 9 | 验证（typecheck/lint/test/build/e2e）+ 真实演示 | 1 小时 |
| 10 | 测试报告 + 提交 | 0.5 小时 |
| **总计** | | **约 7 小时** |

---

## 6. 风险登记（v0.8.2 专项）

| 风险 | 缓解措施 |
|---|---|
| React 19 + Vite 6 在 Windows 上 peer-dep 不匹配 | 精确锁定版本；先验证 `pnpm --dir web install` 成功 |
| Tailwind v4 零配置意外 | v4 `@tailwindcss/vite` 插件处理配置；有缺口时回退到内联 `style` |
| `concurrently` 在 Windows cmd.exe 上引号问题 | 使用 `concurrently -k -n srv,web` 加简单命令字符串；备选 npm-run-all2 |
| Playwright chromium 首次下载慢 | 一次性 `npx playwright install chromium`；记录在测试报告中 |
| dev-web 启动时 E2E 不稳定 | Playwright `webServer.timeout: 60_000` + `reuseExistingServer: !CI` |
| 开发时 CORS | Vite 代理 `/api` → 3000；浏览器仅见 5173 |
| TypeScript strict + JSX + DOM lib 与服务端 tsconfig 冲突 | 独立 `web/tsconfig.json`；根 `tsconfig.json` 排除 `web/` |
| 工具链碎片化（web 有独立 deps + lockfile）| 按 v0.8-design §2.2 可接受；用户仅运行一次 `pnpm --dir web install` |

---

## 7. 文件清单

### 7.1 新建（16 个代码文件 + 3 个文档 + 2 个配置 = 21 个）

| 路径 | 行数（估算）| 用途 |
|---|---|---|
| `web/package.json` | ~25 | web 依赖清单 |
| `web/vite.config.ts` | ~20 | React + Tailwind 插件 + 代理 |
| `web/tsconfig.json` | ~30 | JSX + DOM lib + strict |
| `web/tsconfig.node.json` | ~20 | 用于 vite.config.ts 自身 |
| `web/index.html` | ~15 | SPA 入口 HTML |
| `web/src/main.tsx` | ~10 | React 19 createRoot |
| `web/src/App.tsx` | ~30 | BrowserRouter + 4 个路由 |
| `web/src/index.css` | ~5 | Tailwind 导入 |
| `web/src/vite-env.d.ts` | ~5 | Vite 客户端类型 |
| `web/src/components/MainPage.tsx` | ~100 | 真实会话列表 + 开始按钮 |
| `web/src/components/SessionPage.tsx` | ~15 | 占位页 |
| `web/src/components/HistoryPage.tsx` | ~15 | 占位页 |
| `web/src/components/SettingsPage.tsx` | ~15 | 占位页 |
| `web/src/components/shared/Header.tsx` | ~25 | 通用 header |
| `web/src/lib/api.ts` | ~30 | fetch 封装 |
| `web/src/lib/types.ts` | ~15 | SessionApi 镜像 |
| `web/src/i18n/strings.ts` | ~20 | 中文字符串（英文桩）|
| `playwright.config.ts` | ~25 | E2E 配置 |
| `tests/e2e/main.spec.ts` | ~30 | E2E #1 |
| `docs/sprint/v0.8.2-scope.md` | 本文件 | |
| `docs/sprint/v0.8.2-design.md` | ~250 | 设计细节 |
| `docs/sprint/v0.8.2-test-report.md` | 收尾时撰写 | |

### 7.2 修改（5 个）

| 路径 | 变更 |
|---|---|
| `package.json` | 添加 `concurrently` devDep + 4 个脚本 + 扩展 `typecheck` 为 3 次 `tsc` 调用 |
| `biome.json` | 添加 `web/dist`、`web/node_modules`、`web/.vite`、`test-results`、`playwright-report` 到 `files.ignore` |
| `.gitignore` | 添加 `web/dist/`、`web/node_modules/`、`web/.vite/`、`test-results/`、`playwright-report/`、`tests/e2e/.auth/` |
| `docs/ARCHITECTURE.md` | §11 变更记录添加 v0.8.2 条目 |
| （`src/` 无源代码变更）| v0.8.2 为纯前端 + 工具链 |

---

## 8. 下一步

- **v0.8.3**：SSE turn loop 端到端 —— `SessionPage` 消费 `/api/sessions/:id/stream`，累积 `text-chunk` 事件，通过 query param 发送用户输入。约 2 天。
- **v0.8.4**：`HistoryPage` 详情（扩展服务端 `GET /api/sessions/:id` 包含 `messages[]`）+ `SettingsPage` 接线（PUT /api/settings + USER.md 原子写）。约 2 天。
- **v0.8.5**：打磨 —— 组件 L1 测试、完整 i18n、深色模式、1280×720 媒体查询、Playwright trace 收集、server.ts 生产 SPA fallback。约 1 天。
