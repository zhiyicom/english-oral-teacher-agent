# Sprint v0.8.5 范围 — 打磨 + 流式对话

> **Sprint**: v0.8.5（v0.8 Web UI 的 5 个子 sprint 之第 5 个，收尾打磨）
> **父范围文档**: [`docs/sprint/v0.8-scope.md`](./v0.8-scope.md) §2.1 v0.8.5 块
> **上一 sprint**: v0.8.4（commit `2d78d67` + `6ba8840`）— HistoryPage + SettingsPage 已完成
> **分支**: `main`
> **日期**: 2026-06-13

---

## 背景

v0.8.1–v0.8.4 已交付全部 4 个页面的核心功能和 6 个 API 端点。v0.8.5 是 v0.8 的最后一个 sub-sprint，专注打磨用户体验：真正流式对话、字体实时生效、共享组件提取、键盘导航、生产模式 SPA 服务。

---

## 1. 范围内

### 1.1 核心项（6 个）

| # | 事项 | 文件 | 备注 |
|---|---|---|---|
| 1 | **真正逐字流式对话** | `src/llm/retry.ts`、`src/agent/turn.ts`、`src/cli.ts`、`web/src/components/SessionPage.tsx` | 新增 `chatStreamWithRetryGen` async generator；`runTurn` 换用流式调用，yield `text-chunk`；CLI stdout 逐字输出；SessionPage 实时累积 delta |
| 2 | **字体大小实时生效** | `web/src/index.css`、`web/src/main.tsx`、`web/src/components/SettingsPage.tsx` | 读取 localStorage → 设置 CSS 变量 `--font-size-base` → Tailwind 全局应用 |
| 3 | **MessageBubble 共享组件** | `web/src/components/shared/MessageBubble.tsx` | 从 SessionPage 和 HistoryPage 提取重复的气泡代码 |
| 4 | **键盘导航增强** | `web/src/components/SessionPage.tsx` | Esc 键离开 session（弹确认）|
| 5 | **Loading 状态升级** | `web/src/components/shared/LoadingSpinner.tsx` | 旋转动画 + 文字，替代纯 "加载中…" |
| 6 | **生产 SPA fallback** | `src/server.ts` | 检测 `web/dist/` → serve 静态文件 + SPA fallback 到 `index.html` |

### 1.2 技术债清理（2 个）

| # | 事项 | 文件 | 备注 |
|---|---|---|---|
| 7 | **修复 14 个预存在 test typecheck 错误** | `tests/agent/`、`tests/llm/` | v0.8.2 记录的遗留问题 |
| 8 | **webServer cleanup hang 修复** | `scripts/dev-web.mjs`、`playwright.config.ts` | Windows 进程树终止优化 |

---

## 2. 范围外

| 事项 | 原因 | 移交至 |
|---|---|---|
| 完整中英双语切换（i18n toggle UI）| 中文为主已够用；en 翻译完整但切换 UI 工作量大 | v0.9+ |
| 深色模式 | CSS 变量架构需先建立 | v0.9+ |
| 1280×720 audit + media query | Tailwind responsive 已基本覆盖 | 必要时修补 |
| Playwright 截图存档 | 手动截图或 CI 时再配 | v1.0 |
| F6 语音 I/O | PRD F6 明确 v0.9 | v0.9 |
| F7 话题库 ≥20 | v1.0 | v1.0 |
| F8 会话归档 .md | v1.0 | v1.0 |

---

## 3. 文件清单

### 新建（2 个）

| 路径 | 用途 |
|---|---|
| `web/src/components/shared/MessageBubble.tsx` | 共享消息气泡组件 |
| `web/src/components/shared/LoadingSpinner.tsx` | Loading 动画组件 |

### 修改（12 个）

| 路径 | 变更 |
|---|---|
| `src/llm/retry.ts` | 新增 `chatStreamWithRetryGen()` async generator；保留 `chatStreamWithRetry()` 兼容 |
| `src/agent/turn.ts` | `runTurn()` 新增 `text-chunk` 事件类型；使用 `chatStreamWithRetryGen` 替代 `chatStreamWithRetry`；保留 `student-text` 向后兼容 |
| `src/cli.ts` | `forwardEvent()` 新增 `text-chunk` 处理（stdout 逐字输出）|
| `src/server.ts` | 新增生产 SPA fallback：检测 `web/dist/` → serveStatic + SPA index.html 兜底 |
| `web/src/components/SessionPage.tsx` | 使用 MessageBubble + LoadingSpinner；处理 `text-chunk` SSE 事件实时追加；Esc 键处理 |
| `web/src/components/HistoryPage.tsx` | 使用 MessageBubble + LoadingSpinner |
| `web/src/components/SettingsPage.tsx` | 字体大小保存后实时写入 CSS 变量 |
| `web/src/index.css` | 添加 `--font-size-base` CSS 变量 + Tailwind `text-base` 映射 |
| `web/src/main.tsx` | 启动时读取 localStorage 字体大小设置 CSS 变量 |
| `scripts/dev-web.mjs` | 添加 SIGTERM 进程树清理 |
| `playwright.config.ts` | 适配 webServer cleanup |
| `docs/ARCHITECTURE.md` | 变更记录 |

---

## 4. 完成定义

| # | DoD | 验证 |
|---|---|---|
| 1 | SSE 输出 text-chunk 事件，浏览器逐字渲染 | 手动 + E2E |
| 2 | 字体大小拖动后实时生效 | 手动 |
| 3 | MessageBubble 在 SessionPage 和 HistoryPage 中复用 | typecheck + 手动 |
| 4 | Esc 键弹确认后离开 session | 手动 |
| 5 | Loading 状态显示动画 | 手动 |
| 6 | `pnpm build && pnpm serve` → 浏览器 localhost:3000 看到 SPA | 手动 + curl |
| 7 | `pnpm typecheck` 0 错误（含 test 项目）| CLI |
| 8 | `pnpm lint` 0 错误 | CLI |
| 9 | `pnpm test` 全部通过 | CLI |
| 10 | `pnpm test:e2e` 6/6 | CLI |
| 11 | `pnpm --dir web build` OK | CLI |
| 12 | 文档：scope + design + test-report | 文件 |

---

## 6. v0.8.5 后打磨（2026-06-14）

v0.8.5 交付后实际测试发现 Phase 推进效果不理想，追加以下打磨项：

| # | 事项 | 文件 |
|---|---|---|
| 13 | System Context 阶段指令注入：每个阶段的完整行为指令写入动态块，替代 SOUL.md 间接引用 | `src/agent/context-injector.ts` |
| 14 | 用户消息前缀注入：`[Phase: END — say goodbye now]` prepend 到用户消息（浅拷贝，不修改原始 history）| `src/agent/turn.ts` |
| 15 | 话题库注入 system prompt：`topic-library.md` 作为 `# TOPIC_LIBRARY` 部分发送给 LLM | `src/prompts/loader.ts` |
| 16 | 沉默跳过 WRAP_UP 保护：silence ≥10 min + 跳过 WRAP_UP → 补一轮总结 | `src/agent/turn.ts` |
| 17 | 30 分钟优雅结束：time-based END 走 LLM 告别轮不再硬终止 | `src/agent/turn.ts` |
| 18 | DEBUG_LOG_LLM 调试工具：完整 LLM 请求记录到文件 | `src/llm/debug-log.ts` |
| 19 | SOUL.md 4 阶段完整行为指引 + WARM_UP 跨会话多样化 | `prompts/SOUL.md` |
| 20 | AGENTS.md 修正：去除"5-15 min"和"无跨会话记忆"陈旧描述 | `prompts/AGENTS.md` |
| 21 | RUN_LIVE_LLM 优先级修复：live 模式优先于 fixtures 目录 | `src/server.ts`、`src/cli.ts` |
| 22 | `prompts/topic-library.md`：A1/B1/B2 三级英语口语话题库 | `prompts/topic-library.md` |
