# Sprint v1.0.1 范围 — 功能优化打磨

> **Sprint**: v1.0.1（v0.9 语音功能之外的优化打磨）
> **上一 sprint**: v0.9（F6 语音 I/O）
> **分支**: `main`
> **日期**: 2026-06-15

---

## 背景

v0.9 实现了语音输入输出。在测试过程中发现多项需要打磨的问题：Phase 推进不够强制、话题追踪不准确、会话结束流程不完整、LLM 约束力不够强。本 sprint 集中修复这些问题。

---

## 1. 范围内

### Phase 推进增强

| # | 事项 | 文件 |
|---|---|---|
| 1 | 阶段首次切换时注入完整 Context 文本前缀（打断 LLM 惯性）| `src/agent/turn.ts` |
| 2 | phases.md MAIN_ACTIVITY 强制选题指令：优先选讨论次数最少的话题 | `prompts/phases.md` |
| 3 | topic-library.md 新增"检查 Active topics 选最少讨论次数"硬性约束 | `prompts/topic-library.md` |
| 4 | END 阶段后再发消息直接结束会话，防止无限告别循环 | `src/agent/turn.ts` |
| 5 | 30 分钟到不再硬终止，走 LLM 告别轮 | `src/agent/turn.ts` |
| 6 | 沉默跳过 WRAP_UP 时补一轮总结（catch-up WRAP_UP）| `src/agent/turn.ts` |

### 话题追踪修复

| # | 事项 | 文件 |
|---|---|---|
| 7 | 话题库完全重建：30 个话题对齐 text-library.md 分类体系 | DB `topics` 表 |
| 8 | topic-matcher 评分改进：countScore + keyword-hit ratio | `src/agent/topic-matcher.ts` |
| 9 | `prompts/topic-library.md` 从 DB 自动生成 | `src/server.ts` |
| 10 | Web UI 话题库编辑器（`/topics` 路由）| `web/src/components/TopicLibraryPage.tsx` |
| 11 | GET/PUT /api/topics 端点 | `src/server.ts` |

### 会话记忆增强

| # | 事项 | 文件 |
|---|---|---|
| 12 | lastReview 动态查询（每次新会话首轮查 DB，非启动时缓存）| `src/server.ts` |
| 13 | Web 会话结束时 summarize + markEnded + embedding 完整流水线 | `src/server.ts` |
| 14 | 自动从摘要提取学生信息更新 USER.md（profile-extractor）| `src/agent/profile-extractor.ts` |
| 15 | updateUserSettings 扩展支持 interests + bodyAppend | `src/prompts/loader.ts` |
| 16 | "结束本次" 按钮发送 `stop` 匹配 STOP_REGEX | `web/src/components/SessionPage.tsx` |

### UI 与交互

| # | 事项 | 文件 |
|---|---|---|
| 17 | 侧边栏会话删除功能（hover × 按钮 + 确认 + DB 删除）| `web/src/components/SessionSidebar.tsx` |
| 18 | DELETE /api/sessions/:id 端点（ON DELETE CASCADE）| `src/server.ts`、`src/storage/sessions.ts` |
| 19 | 全局 Enter 键发送消息（文本区未聚焦时也可用）| `web/src/components/SessionPage.tsx` |
| 20 | tool 标签从显示和 TTS 中过滤掉 | `web/src/components/SessionPage.tsx` |

### Prompt 整理

| # | 事项 | 文件 |
|---|---|---|
| 21 | AGENTS.md 合并入 SOUL.md 消除重复 | `prompts/SOUL.md`、`prompts/AGENTS.md` |
| 22 | 阶段指令移至可编辑 `prompts/phases.md` | `src/prompts/loader.ts`、`prompts/phases.md` |
| 23 | DEBUG_LOG_LLM 调试工具 + summarization 日志 | `src/llm/debug-log.ts` |
| 24 | RUN_LIVE_LLM 优先级修复 | `src/server.ts`、`src/cli.ts` |
| 31 | **B4 落地**：从 system prompt 移除 `# TOPIC_LIBRARY` 全文（~4k token），`topic_select` 工具负责选题 | `src/prompts/loader.ts` |
| 32 | **Iron rule #7（无 emoji）**：SOUL 加 "no emoji" 铁律 + SessionPage `stripInternal()` 同时剥 `<tool>` 和 emoji 作为硬保证 | `prompts/SOUL.md`、`web/src/components/SessionPage.tsx` |
| 33 | **Option C 显式纠错**：phases.md 三阶段差异化纠错策略——WARM_UP 不纠；MAIN_ACTIVITY 显式简短纠错 + 调 `mark_mistake`；WRAP_UP 总结今日 1-2 个错点 | `prompts/phases.md`、`prompts/AGENTS.md` |

---

## 2. 范围外

| 事项 | 原因 |
|---|---|
| 语音功能（STT/TTS/快捷键设置）| 属于 v0.9 |
| 移动端适配 | v1.0+ |
| 更多话题库内容 | 已通过 Web UI 支持编辑 |
| 语义级别话题匹配（embedding）| v1.0+ |

---

## 3. 文件清单

### 新建

| 文件 | 用途 |
|---|---|
| `prompts/phases.md` | 可编辑阶段指令文件 |
| `prompts/topic-library.md` | LLM 话题参考（从 DB 生成）|
| `src/agent/profile-extractor.ts` | 摘要提取学生新信息 |
| `src/llm/debug-log.ts` | LLM 请求调试日志 |
| `web/src/components/TopicLibraryPage.tsx` | 话题库编辑界面 |
| `web/src/components/VoiceInput.tsx` | 语音输入按钮（v0.9）|
| `web/src/components/HotkeyInput.tsx` | 快捷键设置组件 |

### 修改

| 文件 | 主要变更 |
|---|---|
| `src/agent/turn.ts` | Phase 强制前缀、END 立即结束、catch-up WRAP_UP、text-chunk 流式、phase_end 原因 |
| `src/agent/context-injector.ts` | 阶段指令注入动态块 |
| `src/agent/state-machine.ts` | PhaseTransition.reason 扩展 |
| `src/agent/topic-matcher.ts` | countScore 评分 + 阈值 0.1 |
| `src/server.ts` | 动态 lastReview、DELETE session、GET/PUT topics、summarize 流水线、regenerateTopicLibrary、RUN_LIVE_LLM 优先级 |
| `src/prompts/loader.ts` | loadPhaseInstructions、updateUserSettings 扩展、topic-library 注入 |
| `src/cli.ts` | text-chunk 事件、RUN_LIVE_LLM 优先级 |
| `src/storage/sessions.ts` | delete 方法 |
| `web/src/App.tsx` | Sidebar 布局 + /topics 路由 |
| `web/src/components/SessionPage.tsx` | 全局 Enter、tool 过滤、TTS、VoiceInput、sidebar 适配 |
| `web/src/components/SessionSidebar.tsx` | 新建组件：会话列表 + 删除 + 设置链接 |
| `web/src/components/SettingsPage.tsx` | 语音控件启用、快捷键设置 |
| `web/src/components/HistoryPage.tsx` | 移除返回链接 |
| `prompts/SOUL.md` | 阶段指令 + 合并 AGENTS |
| `prompts/AGENTS.md` | 精简为工具卡片 |
| `prompts/phases.md` | 阶段 Context + Reminder |
| `tests/agent/topic-matcher.test.ts` | 匹配分数断言更新 |
| `tests/server/app.test.ts` | DELETE + topics 测试 |
| `src/agent/retrieval.ts` | lastReview 过滤 `length(summary) > 30` |
| `web/src/main.tsx` | 启动时恢复 localStorage |
| `docs/sprint/v1.0.1-design.md` | Phase 推进 Round 3 设计文档 |

### Phase 推进 Round 3（2026-06-16）

| # | 事项 | 文件 |
|---|---|---|
| 25 | MAIN_ACTIVITY 自动调用 `topic_select` 工具选题（替代文本指令）| `src/agent/turn.ts` |
| 26 | WARM_UP 独立消息 + 完整摘要文本（关联而非避开上次话题）| `src/agent/turn.ts` |
| 27 | lastReview 过滤无效摘要（`length > 30` 跳过 `summarization failed`）| `src/agent/retrieval.ts` |
| 28 | localStorage 双重持久化（`data/preferences.json` + `main.tsx` 启动恢复）| `src/server.ts`、`web/src/main.tsx` |
| 29 | 热键持久化修复（lazy init + onChange 即时写入 + 服务端兜底）| `web/src/components/SettingsPage.tsx` |
| 30 | `prompts/phases.md` + `prompts/SOUL.md`：WARM_UP 改为关联式而非避开式 | `prompts/phases.md`、`prompts/SOUL.md` |

---

## 7. Backlog

| # | 事项 | 说明 | 优先级 |
|---|---|---|---|
| B1 | **话题库命中率提升** | 当前 4/30 话题有讨论记录。大量实际会话关键词（science、biology、british magazine、delta force 等）无法匹配已有话题。方向：扩展关键词 vs 动态自增长 vs 语义匹配，待评估 | 中 |
| B2 | 话题库动态自增长 | 摘要关键词自动创建新话题，但需解决 system prompt 膨胀问题（话题上限、淘汰机制）| 低 |
| B3 | 非 MAIN_ACTIVITY 阶段的 topic_select 调用 | 当前仅在 MAIN_ACTIVITY 阶段自动调用，WARM_UP 手动选题 | 低 |
| B4 | **移除 system prompt 中的 # TOPIC_LIBRARY 全文** | ~~已经通过 `topic_select` 工具选题，~500 行话题库全文（4000+ token）变为冗余。改为在 MAIN_ACTIVITY Context 中加"话题枯竭时调 topic_select 工具"指令即可~~ **✅ 已落地**（commit `1660792`，见 §1 #31）。loader.ts 的 `buildSystemString` 不再注入 `# TOPIC_LIBRARY`；`prompts/topic-library.md` 仍由 Web UI 编辑器自动重新生成，保留供人参考。 | 中 |
