# Sprint v1.0.5 — Test Report

> Sprint: v1.0.5 — Topic selection 拓宽 + 反 # STUDENT 抓取
> Scope: [v1.0.5-scope.md](./v1.0.5-scope.md) / Design: [v1.0.5-design.md](./v1.0.5-design.md)（原安装器前置 4 段设计推迟到 v1.0.5.1+，见 scope §2）
> Date: 2026-06-28
> Tester: Claude (auto)
>
> **Note**: 本报告对应 v1.0.5 实际完成的 2 段（§A + §B）。原 v1.0.5 scope 定义的"安装器前置 4 段"未在本 sprint 落地，已推迟到 v1.0.5.1+。

## Summary

| 段 | 名称 | 状态 | 测试 |
|---|---|---|---|
| §A | phases.md 反 # STUDENT 抓取禁令 | ✅ Done | N/A（纯 prompt 文档，无代码） |
| §B | topic_select tool 返回 keywords[] + description title | ✅ Done | 4/4 新增测试 pass |
| Docs | CHANGELOG + ARCHITECTURE §2.5/§11 + tools.md | ✅ Done | — |

## §A — phases.md 提示词禁令

**改动**：`prompts/phases.md` MAIN_ACTIVITY Context 加 1 行禁令 + per-turn Reminder 追加 "(NEVER pick from # STUDENT interests directly)"。

**验证**：
- 无代码改动，无需 typecheck / 测试
- 静态读回确认两条修改都到位
- 推理：禁令句直接告诉 LLM "profile 不是 topic source"，与 v1.0.3 §1.3 的 WARM_UP 兴趣钩子（profile 用来调开场）职责分离，互不冲突

## §B — topic_select tool 返回拓宽

**改动**：
- `TopicSelectResult` interface 新增 `keywords: string[]` 字段
- tool `execute()` 把 `title` 从 `winner.name` 改为 `winner.description?.trim() || winner.name`
- tool description 同步说明新返回 + 显式说 "do NOT mine # STUDENT interests for topic material"

**新增测试**（`tests/agent/tools/topic-select.test.ts`）：

| 测试 | 验证 | 结果 |
|---|---|---|
| `returns the full keywords list of the chosen topic` | 透传 `Topic.keywords` 给 LLM | ✅ pass |
| `uses the topic description as title when present, instead of the raw slug` | title 优先用 description | ✅ pass |
| `falls back to slug when description is null or empty/whitespace` | fallback 路径（含 `description: '   '`）| ✅ pass |
| `returns an empty keywords array when the chosen topic has no keywords` | 空数组边界 | ✅ pass |

**既有测试**（未改动）：
- 14 个老测试中有 13 个继续 pass
- 1 个 fail：`returns { error } when hard exclude removes every topic` (line 83)

**Pre-existing fail 根因分析**：
- 测试 setup：3 个 topic 都在过去 30 天内被讨论过，期望 tool 返回 `{ error: 'No topics available after hard exclude' }`
- 实际：tool 返回了成功结果（其中一个 topic）
- 原因：test 假设 `new Date()` 是 2026-06-10（与 `NOW` 常量对齐），但 vitest 跑测试时 `new Date()` 是实际当前时间 2026-06-28
- 计算：3 个 topic 中 `sports` (20 天前) < cutoff (currentTime - 30 天) → 在 window 内被 exclude
- 但 `minecraft` (5 天前) 和 `school` (10 天前) **不在** 30 天 window 内（因为今天是 6/28，30 天前是 5/29，5/18 和 6/08 都早于 5/29？— 让我重算）
- 实际：今天 2026-06-28，cutoff = 2026-05-29
  - minecraft 5 天前 = 2026-06-23 → 2026-06-23 > 2026-05-29 → **不在** hard-exclude window → 进入 pool
  - school 10 天前 = 2026-06-18 → 2026-06-18 > 2026-05-29 → **不在** window → 进入 pool
  - sports 20 天前 = 2026-06-08 → 2026-06-08 > 2026-05-29 → **不在** window → 进入 pool
- 实际：**3 个都不应该被 hard-exclude**，应该全进 pool 并正常返回 1 个 winner
- 测试 fail 是因为 **fixture 的 stat helper 写 `daysAgo: 20` 期望是 "in 30-day window"，但实际 `Date.now() - 20 days` 是 6/08，不在 30 天 window 内**
- 修复方案（不在本 sprint scope）：把 stat daysAgo 改成 35/40/45 才能确保 hard-exclude 触发

**结论**：pre-existing fail 与 v1.0.5 §B 改动**完全无关**——baseline `d577292` 也 fail。已在 baseline run 确认（`git stash` 后 `pnpm test` 同样 fail）。**v1.0.5 §B 零回归**。

## Verify: typecheck / lint / web build

```
$ pnpm typecheck
Already up to date
Done in 544ms using pnpm v11.11.1
$ tsc --noEmit && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p web/tsconfig.json
(exit 0, 0 errors)

$ pnpm --dir web build
✓ 53 modules transformed.
dist/index.html                   0.43 kB │ gzip:  0.29 kB
dist/assets/index-CZeeYdJw.css   16.65 kB │ gzip:  4.17 kB
dist/assets/index-mpABtHRc.js   261.58 kB │ gzip: 82.34 kB
✓ built in 1.64s
```

## Verify: full test suite

`pnpm test` 整体 400/434 pass（5 文件 34 个 fail 全部 baseline d577292 已存在 pre-existing）：
- `tests/agent/cli-integration.test.ts` — 22 fail (pre-existing CLI/state-machine fixture drift)
- `tests/agent/retrieval.test.ts` — 9 fail (pre-existing)
- `tests/cli/repl.test.ts` — 1 fail (pre-existing)
- `tests/llm/retry.test.ts` — 1 fail (pre-existing flaky)
- `tests/agent/tools/topic-select.test.ts` — 1 fail (pre-existing, line 83 fixture)

**v1.0.5 §B 引入测试: 4 个全部 pass，零回归**。

## Live smoke (建议手动)

下次开新 session 时观察：
1. LLM 进入 MAIN_ACTIVITY → 是否调 `topic_select` tool
2. 工具返回里 `keywords` 数组是否含 ~22 个具体英文词
3. LLM 是否还在绕过 tool 抓 `# STUDENT` 兴趣
4. 话题多样性是否比 2026-06-28 session `dc50b481` 更好

预期：LLM 拿到 `keywords: ["morning","afternoon","breakfast",...]` + `title: "日常生活习惯"` 后会用 `breakfast` 当开场 → "What did you have for breakfast today?" 这种具体开场，不再"你可以去世界上任何地方"。

## Files changed

| File | Change |
|---|---|
| `src/agent/tools/topic-select.ts` | TopicSelectResult 加 `keywords: string[]`；title 用 description；description 更新 |
| `tests/agent/tools/topic-select.test.ts` | 新增 4 个 v1.0.5 §B 单元测试 |
| `prompts/phases.md` | MAIN_ACTIVITY Context 加禁令；Reminder 追加 never-#STUDENT 子句 |
| `prompts/tools.md` | topic_select 返回描述更新（含 keywords[] + description-based title）|
| `CHANGELOG.md` | 新增 v1.0.5 段（§A + §B）|
| `docs/ARCHITECTURE.md` | §2.5 tool registry + §11 changelog v1.0.5 行 |

## Backlog 不动

- v1.0.5 §B 提到的 fixture bug（topic-select line 83）：`stat('sports', 1, 20)` 改成 `stat('sports', 1, 40)` 等；不阻塞 v1.0.5 release
- pre-existing 34 个 fail：已记入 backlog (B6/B7/B9)
