# Sprint v1.0.7 设计 — 语音输入错误提示修复

> **Sprint**: v1.0.7
> **依赖**: v1.0.6（已合 main）
> **日期**: 2026-07-06
> **范围**: [v1.0.7-scope.md](./v1.0.7-scope.md)
> **核心约束**：业务代码零改动（仅前端 UI bug 修复）

---

## 1. 现状盘点

### 1.1 触发场景

v1.0.6 §1.13 在 `web/src/components/VoiceInput.tsx` 添加了 Web Speech API 错误提示，但实施粗糙：

```ts
// v1.0.6 — 实施代码
const [showEdgeHint, setShowEdgeHint] = useState(false)
// ...
rec.onerror = () => {
  const err = rec.error ?? 'unknown'
  console.error(`[VoiceInput] SpeechRecognition error: ${err}`)
  setErrorMsg(err)
  setState('error')
  recognitionRef.current = null
  if (err === 'unknown' || err === 'network') {
    setShowEdgeHint(true)
  }
  setTimeout(() => {
    setState('idle')
    setErrorMsg(null)
  }, 2500)
}
// ...
return (
  <>
    <button ...>🎤</button>
    {showEdgeHint && (
      <span className="text-xs text-amber-600">
        Speech unavailable in Chrome. Try Microsoft Edge.
      </span>
    )}
  </>
)
```

### 1.2 暴露的两个问题

#### Bug A：误导性文案

| 场景 | 当前提示 | 实际原因 |
|---|---|---|
| 国内版 Edge + 网络抖动 | "Try Microsoft Edge" | 用户已经在用 Edge，问题不是浏览器 |
| 拒绝麦克风权限 | "Try Microsoft Edge" | 实际是权限问题 |
| 浏览器全局禁用语音识别 | "Try Microsoft Edge" | 实际是浏览器设置问题 |
| 启动后静默不说话 | "Try Microsoft Edge" | 实际是没说话（`no-speech`）|

W3C SpeechRecognitionErrorEvent 定义 6 个错误码 + Chromium 扩展 4 个：

| 错误码 | 含义 |
|---|---|
| `no-speech` | 没有检测到语音 |
| `aborted` | 主动 abort() 打断 |
| `audio-capture` | 麦克风获取失败 |
| `not-allowed` | 用户拒绝权限 |
| `bad-grammar` | SRGS 语法错误（开发者问题）|
| `language-not-supported` | lang 不支持 |
| `network` (Chromium 扩展) | 云端不可达 |
| `service-not-allowed` (Chromium 扩展) | 浏览器全局禁用 |
| `phrase-too-long` (Chromium 扩展) | 单段太长 |
| `audio-busy` (Chromium 扩展) | TTS 占用音频设备 |

当前实现一刀切 = "Try Microsoft Edge"，对所有错误码都成立。

#### Bug B：布局挤压

`<VoiceInput>` 用 `<>...</>` 平级渲染 button + span，外层是 flex：

```tsx
// web/src/components/SessionPage.tsx:574-586
{!ended && (
  <div className="flex gap-2 border-t pt-3">
    <VoiceInput ... />
    <textarea ... />
    <button ...>发送</button>
  </div>
)}
```

`<VoiceInput>` 内部:
```tsx
<>
  <button>🎤</button>
  {showEdgeHint && <span>Speech unavailable in Chrome. Try Microsoft Edge.</span>}
</>
```

→ flex 容器把 `<button>`, `<span>`, `<textarea>`, `<button>发送</button>` 排成一行，提示把 textarea 挤到右边。

### 1.3 已有基础设施

| 资源 | 用途 |
|---|---|
| `web/src/i18n/strings.ts` | 集中管理所有前端文案（v0.8 起）|
| `data-testid` 属性 | 多个组件已用，便于测试和定位 |
| `setState` + `setTimeout` | 已有 2.5 秒自动消失机制（state 部分）|
| Tailwind CSS | 已有 `text-amber-600`、`text-xs`、`text-center`、`pb-2` 等工具类 |

---

## 2. 设计目标

1. **§1.1 错误码细分**：6 类错误码 → 6 条不同文案，用户能看懂发生了什么
2. **§1.2 位置修复**：提示从行内移到 border-t 横线上方居中，不挤压输入框
3. **§1.3 清理**：删除 "Try Microsoft Edge" 误导文案

**核心约束**：
- 业务代码（src/）零改动
- 不引入新依赖
- 不破坏现有 happy path（正常识别时无任何提示）
- 2.5 秒自动消失机制保留

---

## 3. §1.1 — 错误码 → 细分提示

### 3.1 映射表

| 错误码 | STRINGS key | 中文文案 |
|---|---|---|
| `audio-capture` | `voiceErrorAudioCapture` | 无法访问麦克风 |
| `not-allowed` | `voiceErrorNotAllowed` | 需要麦克风权限 |
| `service-not-allowed` | `voiceErrorServiceNotAllowed` | 浏览器禁用了语音识别 |
| `network` | `voiceErrorNetwork` | 识别服务不可达，请检查网络 |
| `no-speech` | `voiceErrorNoSpeech` | 没有听到声音 |
| `language-not-supported` | `voiceErrorLangNotSupported` | 暂不支持该语言 |
| `unknown` / 其他 | `voiceErrorUnknown` | 语音输入出错，请重试 |

### 3.2 函数签名

```ts
// web/src/components/VoiceInput.tsx（内部）
function hintForError(err: string): string {
  switch (err) {
    case 'audio-capture':
      return STRINGS.voiceErrorAudioCapture
    case 'not-allowed':
      return STRINGS.voiceErrorNotAllowed
    case 'service-not-allowed':
      return STRINGS.voiceErrorServiceNotAllowed
    case 'network':
      return STRINGS.voiceErrorNetwork
    case 'no-speech':
      return STRINGS.voiceErrorNoSpeech
    case 'language-not-supported':
      return STRINGS.voiceErrorLangNotSupported
    default:
      return STRINGS.voiceErrorUnknown
  }
}
```

`switch` 而不是对象 lookup —— TypeScript 会在新增错误码时给出 exhaustive 检查提示。

### 3.3 i18n 字段

```ts
// web/src/i18n/strings.ts（追加）
voiceErrorAudioCapture: '无法访问麦克风',
voiceErrorNotAllowed: '需要麦克风权限',
voiceErrorServiceNotAllowed: '浏览器禁用了语音识别',
voiceErrorNetwork: '识别服务不可达，请检查网络',
voiceErrorNoSpeech: '没有听到声音',
voiceErrorLangNotSupported: '暂不支持该语言',
voiceErrorUnknown: '语音输入出错，请重试',
```

集中在 `voiceErrorXxx` 命名空间，便于后续国际化时整组替换。

---

## 4. §1.2 — 提示位置修复

### 4.1 VoiceInput 接口扩展

```ts
// 新增 onHint prop
interface VoiceInputProps {
  onResult: (text: string) => void
  onInterim: (text: string) => void
  onHint?: (msg: string | null) => void  // NEW
  lang?: string
  disabled?: boolean
}
```

`onHint(msg | null)` 双向 —— 错误时传 `msg`，清空时传 `null`（重试/超时）。

### 4.2 VoiceInput 内部改造

```tsx
// 旧
rec.onerror = () => {
  const err = rec.error ?? 'unknown'
  setErrorMsg(err)
  setState('error')
  recognitionRef.current = null
  if (err === 'unknown' || err === 'network') {
    setShowEdgeHint(true)
  }
  setTimeout(() => {
    setState('idle')
    setErrorMsg(null)
  }, 2500)
}

return (
  <>
    <button>🎤</button>
    {showEdgeHint && <span>Speech unavailable in Chrome. Try Microsoft Edge.</span>}
  </>
)

// 新
rec.onerror = () => {
  const err = rec.error ?? 'unknown'
  console.error(`[VoiceInput] SpeechRecognition error: ${err}`)
  setErrorMsg(err)
  setState('error')
  recognitionRef.current = null
  onHint?.(hintForError(err))
  setTimeout(() => {
    setState('idle')
    setErrorMsg(null)
    onHint?.(null)
  }, 2500)
}

// start() 开头加：
const start = useCallback(() => {
  if (!SpeechRecognitionCtor || disabled) return
  onHint?.(null)  // 主动重试时清掉旧提示
  // ...
}, [...])

return <button>🎤</button>  // 不再渲染任何 hint
```

### 4.3 SessionPage 输入区结构

旧结构（一行 flex）：
```tsx
{!ended && (
  <div className="flex gap-2 border-t pt-3">
    <VoiceInput ... />
    <textarea ... />
    <button>发送</button>
  </div>
)}
```

新结构（横线在外层，hint 在上方居中）：
```tsx
{!ended && (
  <div className="border-t pt-3">
    {voiceHint && (
      <div className="text-center text-xs text-amber-600 pb-2" data-testid="voice-hint">
        {voiceHint}
      </div>
    )}
    <div className="flex gap-2">
      <VoiceInput onResult={...} onInterim={...} onHint={setVoiceHint} />
      <textarea ... />
      <button>发送</button>
    </div>
  </div>
)}
```

### 4.4 SessionPage state

```tsx
const [voiceHint, setVoiceHint] = useState<string | null>(null)
```

放在 `voiceEnabled` / `voiceSpeed` / `voiceAccent` 附近（同一组 voice 相关 state）。

---

## 5. §1.3 — 清理

### 5.1 删除的代码

| 文件 | 删除内容 |
|---|---|
| `web/src/components/VoiceInput.tsx` | `useState` 中的 `showEdgeHint` |
| `web/src/components/VoiceInput.tsx` | `rec.onerror` 内的 `if (err === 'unknown' \|\| err === 'network') setShowEdgeHint(true)` |
| `web/src/components/VoiceInput.tsx` | `return` 内的 `{showEdgeHint && <span>...</span>}` |
| `web/src/components/VoiceInput.tsx` | 顶部 `STRINGS.voiceNotSupported` 字段未变（unsupported 状态仍用）|

### 5.2 移除的文案

不再使用：
- ~~"Speech unavailable in Chrome. Try Microsoft Edge."~~（v1.0.6 §1.13 加的）

保留：
- `voiceListening`、`voiceNotSupported`、`voiceStart`（其他场景）

---

## 6. 关键不变量

| 不变量 | 如何保证 |
|---|---|
| 业务代码（src/）零改动 | 本 sprint 只动 `web/src/components/VoiceInput.tsx`、`SessionPage.tsx`、`i18n/strings.ts` |
| 现有 happy path 不破坏 | VoiceInput 组件 props 兼容（onHint 是 optional）；正常识别时 onHint 不被调用，SessionPage 的 voiceHint 保持 null |
| 2.5 秒自动消失 | 保留 `setTimeout`，并把 `onHint?.(null)` 加进去 |
| 不引入新依赖 | 只用 React state + 已有的 STRINGS |
| 不破坏麦克风权限流程 | 浏览器自己的权限弹窗不变，只是错误提示文案细分 |
| 输入框宽度 | 外层不再 flex，hint 单独一行；输入框回到正常 `flex-1` 宽度 |

---

## 7. 边界情况

### 7.1 错误码未来扩展

W3C spec 是 stable 的，但 Chromium 可能在未来加新的扩展码。`hintForError` 的 `default` 分支保证未知错误码也能显示"语音输入出错，请重试"，不至于崩或显示 undefined。

### 7.2 提示期间用户重复点麦克风

- 用户看到 "需要麦克风权限" → 立刻点麦克风按钮 → 重试
- 行为：`onHint?.(null)` 在 `start()` 开头调用 → 旧提示立刻消失
- 下次 `onerror` 时再次触发新的 `onHint?.(msg)`

### 7.3 用户从横线下方 textarea 输入文字

- `voiceHint` 只影响顶部分割线上方的小字
- 不影响 textarea 焦点、键盘事件、disabled 状态

### 7.4 多次连续错误

- 每次 `onerror` 都覆盖 `voiceHint`
- 2.5 秒计时器不重置 —— 第一次的 timer 触发后清空，第二次的 timer 也触发空操作（幂等）

### 7.5 abort() 主动打断

- `aborted` 不在 switch case 中 → 走 default → "语音输入出错，请重试"
- 这是预期行为（用户主动 stop 不应该弹错误）
- 进一步优化可在 `start()` 开头加 `onHint?.(null)`，让 stop 时清空（v1.0.7 不做）

---

## 8. 关键文件合约

### 8.1 `web/src/i18n/strings.ts`

追加 7 条字段，key 前缀 `voiceError`，与其他 voice 字段（`voiceListening` 等）相邻。

### 8.2 `web/src/components/VoiceInput.tsx`

| 改动 | 位置 |
|---|---|
| 新增 `onHint` prop | interface VoiceInputProps |
| 新增 `hintForError(err: string): string` | 内部私有函数 |
| 删除 `useState` 中的 `showEdgeHint` | state 区 |
| `rec.onerror` 改造 | onerror handler |
| `start()` 开头加 `onHint?.(null)` | start callback |
| 内部 render 不再含 hint span | return JSX |

### 8.3 `web/src/components/SessionPage.tsx`

| 改动 | 位置 |
|---|---|
| 新增 `useState<string \| null>(null)` | voice 相关 state 区 |
| 输入区结构调整 | line ~574-586 |
| `<VoiceInput>` 增加 `onHint={setVoiceHint}` | prop 列表 |

---

## 9. 验证

### 9.1 静态检查

| 命令 | 期望 |
|---|---|
| `pnpm typecheck` | 0 errors |
| `pnpm --dir web build` | OK |

### 9.2 Manual smoke

按 `v1.0.7-scope.md` §5 的 5 个场景跑：
1. 正常识别 → 无提示
2. 拒绝麦克风权限 → "需要麦克风权限" 上方居中，2.5 秒消失
3. 拔网线识别 → "识别服务不可达，请检查网络"
4. 静默 5 秒 → "没有听到声音"
5. 连续触发 → 旧提示立刻被新提示替换

### 9.3 视觉对比

- **旧**：提示在按钮和 textarea 之间，textarea 被挤窄
- **新**：提示在 border-t 上方，textarea 宽度正常

截图记录在 `docs/sprint/v1.0.7-test-report.md`。

---

## 10. 风险与回退

同 `v1.0.7-scope.md` §7。

---

> **DoD**: §3 错误码映射完成；§4 VoiceInput + SessionPage 改造完成；§5 删除误导文案；§9 验证矩阵全过；不再有 "Try Microsoft Edge" 字样；输入框宽度恢复正常。